# Status Report — 2026-03-01

## What got done today

### BF-3-Bug4 — Prereq x-stacking fix (PR #44, merged)
- **Branch:** `feature/bf-3-gap-expansion` (squash-merged)
- **Problem:** All prereqs placed by gap expansion landed at the same x, stacking on top of each other.
- **Fix:** Pre-filter unplaced prereqs, spread them horizontally centered on the gap node (240px apart = 220px node + 20px gap). `pipeline.ts` ~line 644–660.
- **Test:** gs-024 — asserts multiple prereqs have distinct x values.

### BF-Conn1 — liveObjects width fallback (PR #45, merged)
- **Branch:** `fix/bf-conn1-width-fallback` (squash-merged)
- **Problem:** `liveObjects` in `executeToolCalls` hardcoded width=160/height=80 for all newly created objects. For kg-nodes (220x80), same-batch `connectKnowledgeNodes` computed edge endpoints against wrong bounds — arrows started/ended inside the node.
- **Fix:** Type-aware lookup from `SHAPE_DEFAULTS` in `executor.ts` ~line 293. Imported from `capabilities.ts`.
- **Test:** gs-023 — same-batch place+connect produces endpoints outside the 220px boundary.

### Test suite
- **52 tests passing** (was 51 before today's session)
- `npx tsc --noEmit` clean

---

## What's next — remaining connector/feature bugs

| # | ID | Description | Status | Branch |
|---|----|-------------|--------|--------|
| 1 | BF-3-Bug4 | Prereq x-stacking | **Done** (PR #44) | — |
| 2 | BF-Conn1 | liveObjects width fallback | **Done** (PR #45) | — |
| 3 | BF-Conn2 | Re-snap assessment | **Blocked on manual test** | TBD |
| 4 | BF-Conn3 | `autoConnectKgEdges` | Pending | `fix/bf-conn3-auto-connect` |
| 5 | BF-4 | Self-report batch update | Pending | `feature/bf-4-self-report-batch` |

### BF-Conn2 — manual test needed
BF-Conn1 fixed the root cause (wrong dimensions in liveObjects). BF-Conn2 asks: is a post-execution re-snap pass still needed, or did BF-Conn1 alone fix the visual drift?

**Manual test script:**
1. Open app in Learning Explorer, grade 6, diagnostic flow.
2. Say **"I don't understand ratios"** — check if arrows touch node edges.
3. Say **"I have a gap in unit rates"** — triggers gap expansion; check prereq→gap arrows.
4. Say **"I know how to multiply fractions"** — check any new arrows.
- **Arrows touching edges** → BF-Conn2 is a no-op, skip it.
- **Arrows drifting/inside nodes** → need `fix/bf-conn2-resnap` branch with a post-execution re-snap pass.

### BF-Conn3 — autoConnectKgEdges
Implement `autoConnectKgEdges` as a post-step-7 pipeline function: draw all KG edges between nodes in `kgNodeMap` using `getEdgesAmong`, deduped against existing lines. This replaces the LLM's responsibility for calling `connectKnowledgeNodes` — makes edge drawing deterministic.

**Files:** `src/agent/pipeline.ts`, possibly `src/agent/executor.ts`

### BF-4 — Self-report batch update
Add `kgNodeIds?: string[]` + `autoConfidence?: string` to `askClarification` schema. Pipeline intercept: when these fields are present, apply `updateNodeConfidence` batch directly, skip LLM.

**Files:** `src/agent/tools.ts`, `src/agent/pipeline.ts`, `src/agent/learningExplorerPrompt.ts`

---

## Remaining roadmap after BF-4

From `LEARNING-EXPLORER-REDESIGN.md`, these sprints are still pending:

| Sprint | Description | Key deliverables |
|--------|-------------|-----------------|
| **2.5** | Canvas-native assessment | Right-click KG node context menu ("I don't know this" / "Quiz me"), canvas→agent dispatch |
| **3** | Graph intelligence | Expansion above green / below red nodes, visual layering with animations |
| **4** | Gamified mode + persistence | Focused learning loop (3-question mini-loop per topic), mastery trail pills, Firestore session persistence |
| **Parental controls** | Hook stubs | Firestore schema, `useParentalControls()`, locked screen |

Sprint 2 core (mode selection, provisional coloring, practice loop) is complete. Sprint 2.5 and beyond are untouched.

---

## Key references
- Full redesign plan: `planning_docs/LEARNING-EXPLORER-REDESIGN.md`
- Connector bug investigation: `planning_docs/LEARNING_CONNECTOR_BUGFIX.md`
- Per-bug PR plan: `.claude/plans/clever-drifting-starlight.md`
- Test skill: `.claude/skills/run-tests/SKILL.md`
- Codebase context docs: `.claude/context/`
